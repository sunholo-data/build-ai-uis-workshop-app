# Template Improvements — Upstream Feedback Tracker

**Sources:** CPH Uni AIPLA fork (`cphu/aipla-app`) v0.1 sprint, May 2026 (items #1–#30); gde-ap-agent Track-3 competition fork, 2026-06-02 (items G13–G22, via ailang msg `8e82510d`); downstream fork user, 2026-06-03 (item G23 — sanitize/docs asymmetry); gde-ap-agent fork AP demo polish, 2026-06-03 (items G24–G37, two-round protocol-stack + chat-surface + artefact + cloudbuild feedback memo); CPH Uni AIPLA v1.1.0-feedback round, 2026-06-05 (item G38 — AGUIProvider unmount on token refresh; plan revision for #30 — AIPLA validated `StaticArtefactFrame` spec-compliant path, ships as primary); gde-ap-agent fork A2A discovery debug, 2026-06-05 (item G39 / Friction 15 — Next ingress doesn't proxy `/.well-known/agent.json` on Cloud Run multi-container deploys, silent 404 for A2A crawlers); gde-ap-agent fork long-demo 401 debug, 2026-06-05 (item G40 / Friction 21 — HttpAgent bearer header frozen at mount; Firebase token expires after ~1h and every subsequent runAgent() returns 401); gde-ap-agent fork tool-throw mid-stream debug, 2026-06-06 (item G41 / Friction 22 — `ag_ui_adk` emits both RUN_ERROR via queue path AND RUN_FINISHED via outer try-block when a tool raises; frontend AG-UI state machine throws on the duplicate terminal); gde-ap-agent fork MCP silent-misconfig debug, 2026-06-06 (item G42 / Friction 23 — SKILL.md declares MCP servers not in Firestore; pre-G42 silently-partial agent built and ran); gde-ap-agent fork Gemini Enterprise registration debug, 2026-06-07 (item G43 / Frictions 22-24 — A2A card spec violations: localhost url leak + missing protocolVersion + bare-string extensions reject Discovery Engine registration); gde-ap-agent fork dual-cloudbuild investigation, 2026-06-07 (item G44 — `backend/cloudbuild.yaml` deployment-model choice not discoverable; fork ran 6 months with `trigger-aitana-dev-aitana-v6-backend` FAILing because nothing in the template surfaces "delete this file if you don't have channels/A2A/external MCP callers"); gde-ap-agent fork A2A-INVOKE sprint, 2026-06-07 (item G45 / Frictions 25-27 — A2A discovery-compliant card returns 405 on actual `message/send` invocation; Friction 25 Next.js owns ingress and `/a2a/*` falls through to catch-all 404, Friction 27 `to_a2a()` Starlette sub-app loses lifespan-registered routes when mounted on FastAPI, §2 well-known proxy strips card.url path); gde-ap-agent fork production-debugging 2026-06-07/08 of file-aware A2A traffic from Gemini Enterprise (item G46 / Frictions 29-30 — peer-sent files silently stripped because card advertises only `defaultInputModes:["text"]`; org-scoped GCS bucket binding has no convention; `force_new_version=True` REQUIRED on `A2aAgentExecutor` because GE doesn't send the new-version `X-A2A-Extensions` hint → without flag, ADK runs legacy code path that bypasses interceptors silently; Discovery Engine Agent resource has no `metadata` field for per-registration tenant binding, must use per-deploy env var)  
**Target:** `sunholo-data/ai-protocol-platform` public template  
**Owner:** Mark / Aitana Labs  
**Last Updated:** 2026-06-07

This directory tracks design docs for improvements to the **public template** surfaced by
downstream forks. Each item maps to one or more numbered feedback entries from the AIPLA
sprint's upstream-feedback log. Items resolved in the Aitana platform but not yet pushed to
the template are marked **Sync needed**.

---

## Design Docs

| Doc | Items | Status | Est | Notes |
|-----|-------|--------|-----|-------|
| [template-fork-ergonomics.md](template-fork-ergonomics.md) | #1 #2 #3 #4 #11 #12 + G13–G23 + G39 + G44 | ✅ Sprint A largely shipped (2026-06-05) + G44 docs follow-up (2026-06-07) — **template sync pending** | 5.5d planned; ~3h Sprint A actual; +30min G44 | 15/18 items shipped or pre-existed. Shipped Sprint A: #11b TRANSPORT_FIELD wiring, G15 bare-id expansion, G16 idempotent seeder, G17 demo opt-in, G18 `^\|^` delimiter, G20 `??`→`\|\|`, G23 sanitize-docs Part B. Shipped 2026-06-07: G44 docs-only (preamble rewrite + `docs/ops/deployment-models.md`). Deferred to Sprint B: G13 bootstrap, G14 region-aware Vertex, G21 role discriminator. |
| [template-protocol-defaults.md](template-protocol-defaults.md) | G24–G27 | ✅ Largely implemented in platform (2026-06-05) — **template sync pending** | 3d (~1d shipped; ~2d follow-up) | G24 short-circuit + G26 callback composer shipped with 9 tests; G25 documented in `docs/ops/gotchas.md`; G27 confirmed already v0.9-correct in starter skills. Deferred: deploy-time A2UI validator + starter artefact with vanilla-JS helpers. |
| [template-chat-surface-defaults.md](template-chat-surface-defaults.md) | G22 + G28 + G29 (partial) + G31 + G34 + G38 | ✅ Sprint A shipped (2026-06-05) — **template sync pending** | 4.5d planned; ~3h Sprint A actual | G28 (DocumentHistoryPanel cap), G29 (DefinitionList primitive only — routing deferred), G31 (Workbench tabs + mount preservation), G34 (avatar theme + photoURL), G38 (AGUIProvider contract). G32 N/A (no viewMode buttons in platform). G29 routing + G33 deferred to Sprint B with audit-view port. |
| [template-mcp-apps-artefact-quality.md](template-mcp-apps-artefact-quality.md) | G35 G36 | ✅ G35 shipped (2026-06-05) — **template sync pending**. G36 N/A in template (no demo artefacts to seed) | 1.5d planned; ~1h actual | `_template` dark palette + hostContext.theme consumption + ui/update-theme runtime channel; StaticArtefactFrame emits ui/update-theme on post-init prop change (+4 tests). shared/theme.css deferred (premature with one artefact). |
| [template-auth-token-refresh.md](template-auth-token-refresh.md) | G40 / Friction 21 | ✅ Shipped (2026-06-05) — **template sync pending** | 1d planned; ~1h actual | New `subscribeToIdToken` helper in `lib/firebase.ts` (4 guarded paths) + AGUIProvider mutates `agent.headers.Authorization` in place on every token rotation. HttpAgent stable across rotations; G38 no-unmount contract preserved. +7 unit + 2 contract tests. Fixes the universal "agent dies after 1h" trap any Firebase-Auth fork hits on long sessions. |
| [template-agui-terminal-dedup.md](template-agui-terminal-dedup.md) | G41 / Friction 22 | ✅ Shipped (2026-06-06) — **template sync pending** | 0.5d planned; ~1h actual | `stream_agui_events` in `adk/agui.py` keeps first terminal event (RUN_ERROR XOR RUN_FINISHED) and drops any subsequent terminals with `agui_terminal_dedup` WARNING log. Fixes the universal "tool throws → frontend hard-stop with 'Cannot send event type RUN_FINISHED'" trap every fork hits the first time a tool raises. +6 unit tests. |
| [template-mcp-strict-resolution.md](template-mcp-strict-resolution.md) | G42 / Friction 23 | ✅ All 3 parts shipped (2026-06-06) — **template sync pending** | 0.5d planned; ~2h actual | (a) New `derive_in_process_mcp_base_url()` helper + gotchas.md entry — loopback default (127.0.0.1:$PORT) for any fork auto-seeding in-process MCP servers. (b) `seed_mcp_servers.py` 127.0.0.1 default + `--public-url` flag. (c) `resolve_mcp_tools` raises `McpServerResolutionError` at agent-build with diff naming missing server-ids. Co-designed with G41: missing-server build failures emit clean `RUN_ERROR` (G41 suppresses the spurious follow-up `RUN_FINISHED`). +13 unit tests. |
| [template-a2a-spec-compliance.md](template-a2a-spec-compliance.md) | G43 / Frictions 22-24 | ✅ Shipped (2026-06-07) — **template sync pending** | 1d planned; ~3h actual | 3-part A2A v0.2 spec compliance: (1) backend emits `protocolVersion: "0.2.0"` + `capabilities.extensions[]` as AgentExtension descriptors + X-A2A-Extensions negotiation w/ Vary; (2) frontend proxy rewrites `url` to public origin via `X-Forwarded-Proto`/`X-Forwarded-Host`; (3) `scripts/verify-a2a.sh` 12-check spec probe + `make verify-a2a` target. Forks can now register with Gemini Enterprise on first try. +5 backend + 8 frontend tests. |
| [template-a2a-invocation-bridge.md](template-a2a-invocation-bridge.md) | G45 / Frictions 25-27 | ✅ Shipped (2026-06-08) — **template sync pending** | 1-2d planned; ~3h actual | Closes the discovery-vs-invocation gap left by G43. New `protocols/a2a_invocation.py` with sync `build_a2a_app()` (Friction 27 workaround for `to_a2a()` lifespan-loss-on-mount), `A2AAuthMiddleware` returning JSON-RPC error envelopes. Frontend Next.js `/a2a` rewrites (Friction 25). Well-known proxy preserves card.url path (§2). Cloud Build env-var enable. New `scripts/simulate-a2a-peer.py` (6-step peer probe) + verify-a2a.sh POST probe. +6 backend + 2 frontend tests. After this, peers don't just discover the agent — they can actually invoke skills. |
| [template-a2a-document-support.md](template-a2a-document-support.md) | G46 / Frictions 29-30 | ✅ Shipped (2026-06-08) — **template sync pending** | 1-2d planned; ~3h actual | Two coupled production failure modes on the G45 invocation surface: (A) peer sends a fresh file but card advertises only `defaultInputModes:["text"]`, so GE strips it at the peer side before reaching us (real failure 2026-06-07T22:06 UTC, agent saw `document_ids=[]`); (B) peer asks about existing docs in a curated org bucket but no convention exists for "this deploy is bound to this bucket". Plus Friction 29 (`force_new_version=True` REQUIRED on `A2aAgentExecutor` — GE doesn't send the new-version `X-A2A-Extensions` hint, so without the flag ADK silently runs the LEGACY path that bypasses interceptors entirely) and Friction 30 (Discovery Engine rejects custom `metadata` field on the Agent resource — pivot to env-var binding). Fix: extend card `defaultInputModes` with PDF/DOCX/XLSX/PPTX/ODT/EML/CSV/TXT MIME types + env-var override; new `FileExtractionInterceptor` walks message.parts, validates MIME+size, persists as `doc:{id}.json` artifacts, strips FileParts to prevent double-injection; new `list_org_documents` + `read_org_document` `FunctionTool`s wired to `root_agent` when `A2A_AGENT_DOCUMENTS_BUCKET` env var is set; SA needs `roles/storage.objectViewer` on bound bucket. All env-gated, single-tenant per deploy. Multi-tenant per-call binding deferred until GE sends a peer-identity header. |
| [template-cloudbuild-hardening.md](template-cloudbuild-hardening.md) | #5 #6 #7 #8 #13 #14 G37 | ✅ ALL 7 items shipped (2026-06-05) — **template sync pending** | 1.5d planned; all done | Channel-secret gating + per-project log bucket + CB SA bootstrap + GitHub-admin docs + metadata-server token mint + email-claim diagnostic + mcp-sandbox auto-deploy trigger guidance. |
| [template-auth-hardening.md](template-auth-hardening.md) | #9 #19 #20 #21 | ✅ ALL 4 items shipped (2026-06-05) — **template sync pending** | 1d planned; all done | Firebase Resource Location in gotchas.md; permissions.py empty-string guards; `_ensure_tool_permissions_wildcard` in platform_seed; onSnapshot anonymous-group gates in useDocBrowser + useDocument. |
| [template-tool-opt-out.md](template-tool-opt-out.md) | #22 #25 | ✅ Implemented in platform (2026-06-05) — **template sync pending** | 0.5d | Code + 9 tests live in platform repo; next aitana-template-publish run syncs to public template |
| [template-session-management.md](template-session-management.md) | #26 #27 | ✅ #26 shipped (2026-06-05); #27 still planned — **template sync pending** | 1d planned; 1 of 2 done | `sessions_route.py` uses `APP_NAME` not `skill_id` at both call sites. `POST /api/sessions/{id}/bootstrap` (pre-creates ChatSessionIndex + ADK session) still planned for #27. |
| [template-mcp-apps-artefacts.md](template-mcp-apps-artefacts.md) | #28 #29 #30 | ✅ Largely implemented in platform (2026-06-05) — **template sync pending** | 2d planned; ~6h actual (sprint MCPAPP-AUDIT) | `StaticArtefactFrame` + tests + iframe-guide + ADR-013 + `_template` artefact all shipped; `useSandboxedIframeMessages` deleted per "no fallback" discipline; pairs with template-mcp-apps-artefact-quality.md (sibling) |
| [template-dx-hardening.md](template-dx-hardening.md) | #10 #15 #17 #18 #24 | ✅ ALL 5 items shipped (2026-06-05) — **template sync pending** | 1d planned; all done | `/join/i` unanchored matcher; README API discovery; `/gcs_config` dead-mount removed; `.claude/skills/agent-protocols/` specs; `get-firebase-config.sh` fail-loud check for missing Dockerfile ARGs (#18). |

**Total estimated effort:** ~21.5 days across ten PRs (was 21d; +0.5d on chat-surface-defaults for G38).

---

## Item Index

| # | Short title | Doc | Status |
|---|------------|-----|--------|
| 1 | `seed_skills.py` closed-set DISPLAY_NAMES dict | fork-ergonomics | Planned |
| 2 | `seed_skills.py` pins GCP project to aitana-multivac-dev | fork-ergonomics | Planned |
| 3 | `PLATFORM_OWNER_EMAIL` defaults to platform@aitanalabs.com | fork-ergonomics | Planned |
| 4 | CLI hardcoded as `aiplatform` / Aitana URLs | fork-ergonomics | Planned |
| 5 | `cloudbuild.yaml` non-optional channel secrets | cloudbuild-hardening | Planned |
| 6 | `cloudbuild.yaml` hardcodes Aitana logs bucket | cloudbuild-hardening | Planned |
| 7 | New GCP projects lack legacy Cloud Build SA | cloudbuild-hardening | Planned |
| 8 | Cloud Build v2 repo registration needs GitHub admin | cloudbuild-hardening | Planned |
| 9 | Firebase Resource Location ID set by first Firestore create | auth-hardening | Planned |
| 10 | Anchored `/^join$/i` regex breaks on any rebrand | dx-hardening | Planned |
| 11 | "Aitana" strings embedded in code / protocol URIs | fork-ergonomics | Planned |
| 12 | `_MCP_SANDBOX_URL` points at Aitana's Cloud Run URL | fork-ergonomics | Planned |
| 13 | `gcloud auth print-identity-token` fails under user-managed SA | cloudbuild-hardening | Planned |
| 14 | Minted ID token missing `email` claim — backend silently 403 | cloudbuild-hardening | Planned |
| 15 | Skill-invoke endpoint path not discoverable without reading source | dx-hardening | Planned |
| 16 | Anonymous-group state lives in process memory | — | ✅ Shipped in v6.2.0 (2.11) — **sync template** |
| 17 | `/gcs_config` volume mount wired but nothing reads it | dx-hardening | Planned |
| 18 | `frontend/Dockerfile` silently drops undeclared `NEXT_PUBLIC_*` ARGs | dx-hardening | Planned |
| 19 | `auth/permissions.py` crashes on empty `user_email` | auth-hardening | Planned |
| 20 | `tool_permissions/*` wildcard seeded in LOCAL_MODE but not in prod | auth-hardening | Planned |
| 21 | Frontend `onSnapshot` listeners → `permission-denied` for anonymous users | auth-hardening | Planned |
| 22 | A2UI toolset appended to every skill regardless of `tools: []` | tool-opt-out | **Sync needed** |
| 23 | Chat-page flex column missing `min-h-0` | — | ✅ Fixed in platform (commit `36ee3cd`) — **sync template** |
| 24 | Template should ship vendored protocol specs as a project-local skill | dx-hardening | Planned |
| 25 | Four default tools (artifacts + memory) wired unconditionally | tool-opt-out | **Sync needed** |
| 26 | `GET /api/sessions/{id}/state` uses `skill_id` not `APP_NAME` | session-management | Planned |
| 27 | `ChatSessionIndex` created lazily — iframe pushes pre-first-turn 404 | session-management | Planned |
| 28 | Sandboxed iframe opaque origin — bypass of sandbox-proxy documented | mcp-apps-artefacts | Planned |
| 29 | `wrap_with_iframe_context` defensive framing → model ignores state | mcp-apps-artefacts | Planned |
| 30 | No paved path for static (non-agent-summoned) iframe artefacts | mcp-apps-artefacts | Planned |
| G13 | Deployed-fork bootstrap: Agent Engine / artifact bucket / AI Search datastore / MCP sandbox not auto-created | fork-ergonomics | Planned |
| G14 | `VertexAiSessionService` / `MemoryBankService` use Cloud Run region, not Agent Engine region | fork-ergonomics | Planned |
| G15 | SKILL.md bare ids (`datastore_id`, `bucket`) not expanded to full resource paths | fork-ergonomics | Planned |
| G16 | Skill seeder is create-if-missing only — rotating `PLATFORM_OWNER_UID` strands old rows | fork-ergonomics | Planned |
| G17 | Seven default demo skills opt-out-by-deletion (eight files) instead of `_INCLUDE_DEMO_SKILLS` flag | fork-ergonomics | Planned |
| G18 | `cloudbuild.yaml --set-env-vars` comma-separator silently corrupts list-valued env vars | fork-ergonomics | Planned |
| G19 | Sub-agent wiring requires generated skill UUIDs; templates only know names | fork-ergonomics | Planned |
| G20 | `process.env.X ?? "default"` doesn't fall back on empty-string env vars | fork-ergonomics | Planned |
| G21 | No `role: hub \| specialist` discriminator — specialists render as peer chat tabs | fork-ergonomics | Planned |
| G22 | Net-new UI surfaces (GCS browser, Audit View, doc-panel modes, sidebar accordion, MCP App widget pattern) | chat-surface-defaults | **Resolved by [template-chat-surface-defaults.md](template-chat-surface-defaults.md)** |
| G23 | Sanitize pipeline strips `whoami_smoke.py` / `verify_rules.py` / `_env.py` but leaves six docs + `smoke-deployed.sh` referencing them — dead links for forks | fork-ergonomics | Planned |
| G24 | Two-pass `response_schema` callback as default for structured output — costs a Gemini round-trip per specialist; function-as-schema is faster + model-native | protocol-defaults | Planned |
| G25 | A2UI `updateDataModel` with no `path` field defaults to root-REPLACE — multi-stage skills clobber prior payloads silently | protocol-defaults | Planned |
| G26 | `_composed_after_agent(ctx) -> None` silently drops callback return values — `Content` follow-up events never reach the wire; ship `compose_after_agent_callbacks` helper | protocol-defaults | Planned |
| G27 | A2UI v0.8 Button shape in starter SKILL.md JSON; LLM retries to v0.9 on attempt 2 every run — add deploy-time schema validator | protocol-defaults | Planned |
| #30 (revised) | `StaticArtefactFrame` spec-compliant path proven by AIPLA sprint MCPAPP-SPEC; template flips from "ship hook workaround now, defer StaticArtefactFrame to v1" → "ship StaticArtefactFrame as primary, drop hook" | mcp-apps-artefacts | **Plan revised 2026-06-05** |
| G28 | `DocumentHistoryPanel` defaults open + uncapped — overflows `DocumentPanel` for high-traffic docs | chat-surface-defaults | Planned |
| G29 | A2UI `Row([Text, Text])` has no label-column convention — every scalar JSON Card reads cramped; ship `DefinitionList` primitive | chat-surface-defaults | Planned |
| G30 | `InspectorPanel` shows `result_content` for `emit_*` tools — STOP sentinel instead of payload; audit pane empty on every function-as-schema specialist | chat-surface-defaults | Planned |
| G31 | Right-pane single-slot conditional ladder unmounts MCP App iframes on every `surface_action` — replace with tabbed `Workbench` | chat-surface-defaults | Planned |
| G32 | `DocTab` viewMode buttons assume non-tabbed layout — decorative after Workbench lands; Workbench Document tab gates on stale `viewMode` flag | chat-surface-defaults | Planned |
| G33 | Inline `emit_*` Card duplicates workspace surface Card — two visually-different renderings of the same payload reads as styling bug | chat-surface-defaults | Planned |
| G34 | Chat avatars hardcode Tailwind gradients; `user.photoURL` not threaded from `useAuth` to `MessageBubble` | chat-surface-defaults | Planned |
| G35 | MCP App artefacts hardcode colours, ignore `hostContext.theme` from `ui/initialize` — clash with host shell on theme flip | mcp-apps-artefact-quality | Planned |
| G36 | MCP App artefacts ship with empty/skeletal default state — "open iframe, see nothing interesting" undercuts the protocol's pitch | mcp-apps-artefact-quality | Planned |
| G37 | `infrastructure/mcp-sandbox/artefacts/**` changes don't auto-deploy — manual `scripts/deploy-mcp-sandbox.sh` only; fork retheme appears to not land | cloudbuild-hardening | Planned |
| G38 | `AGUIProvider` unmounts entire subtree on every Firebase ID-token refresh — invisible to upstream (co-located europe-west1) but visible mid-chat flicker for any fork with cross-region backend latency (AIPLA: ~400ms blank) | chat-surface-defaults | Planned |
| G39 | Next.js ingress doesn't proxy `/.well-known/agent.json` on Cloud Run multi-container deploys — A2A crawlers hit the frontend container (404) instead of the backend sidecar; backend `/api/proxy/.well-known/agent.json` works but RFC 8615 mandates the unprefixed URI. One-file fix: `frontend/src/app/.well-known/agent.json/route.ts` that proxies byte-for-byte and preserves `X-A2A-Extensions` header round-trip. | fork-ergonomics | Planned (quick fix, ~30min) |
| G40 / Friction 21 | AGUIProvider freezes the Firebase ID token into HttpAgent headers at mount. Firebase rotates the token after ~1h; every subsequent runAgent() carries the expired bearer and gets 401. Agent appears "broken" mid-session; only workaround is page refresh. Fix: new `subscribeToIdToken(callback)` helper that wraps Firebase `onIdTokenChanged` (+ LOCAL_MODE / anonymous-group / not-configured guards); AGUIProvider mutates `agent.headers.Authorization` in place on each rotation — no agent rebuild, no unmount. Preserves G38 contract. | auth-token-refresh | ✅ Shipped (2026-06-05) |
| G41 / Friction 22 | When a tool raises mid-stream, `ag_ui_adk` emits BOTH RUN_ERROR (via the background queue) AND RUN_FINISHED (via the outer try-block that doesn't know the queue error happened). Frontend `@ag-ui/client` state machine throws "Cannot send event type RUN_FINISHED: The run has already errored with RUN_ERROR". Fix: `stream_agui_events` enforces the AG-UI spec invariant (at most one terminal event per run) — keeps the first terminal, drops subsequent ones with a WARNING log. Symmetric defence in both directions. | agui-terminal-dedup | ✅ Shipped (2026-06-06) |
| G42 / Friction 23 | SKILL.md declares `tool_configs.mcp.servers: [X]` but Firestore `mcp_servers/X` doesn't exist (typo, missed seed, wrong env, malformed config). Pre-G42 the agent built with fewer tools than declared and silently misbehaved at runtime ("agent ignored my prompt"). Fix has 3 parts: (a) `derive_in_process_mcp_base_url()` helper returns `127.0.0.1:$PORT` so any fork auto-seeding in-process MCP servers gets the loopback default for free (public Cloud Run URL would route to frontend container and 404); (b) `seed_mcp_servers.py` default to 127.0.0.1 + `--public-url` flag forces explicit non-loopback overrides; (c) `resolve_mcp_tools` raises `McpServerResolutionError` at agent build with a diff naming the missing IDs + a pointer at the seed script. | mcp-strict-resolution | ✅ All 3 parts shipped (2026-06-06) |
| G43 / Frictions 22-24 | Gemini Enterprise A2A registration rejects the template's `/.well-known/agent.json` card with 3 coupled spec violations: (1) `url` field leaks `localhost` because backend is a sidecar that can't know its public URL; (2) missing `protocolVersion` field → Discovery Engine `INVALID_ARGUMENT`; (3) `capabilities.extensions[]` are bare strings instead of `AgentExtension` objects → "unexpected instance type". Fix: backend emits `protocolVersion`+`AgentExtension[]`+X-A2A-Extensions negotiation; frontend proxy rewrites `url` via X-Forwarded-*; new `verify-a2a.sh` 12-check probe gates CI BEFORE the next Gemini Enterprise registration attempt. | a2a-spec-compliance | ✅ Shipped (2026-06-07) |
| G44 | Dual `cloudbuild.yaml` deployment-model choice (single-service sidecar vs paired sidecar+standalone) not surfaced by the template; downstream gde-ap-agent fork ran 6 months with `trigger-aitana-dev-aitana-v6-backend` FAILing on every push because nothing in `backend/cloudbuild.yaml` says "delete me if you don't run channels / external A2A / external MCP callers". Fix: 25-line "DELETE THIS FILE IF…" preamble in `backend/cloudbuild.yaml` + new `docs/ops/deployment-models.md` (TL;DR table, model A vs B, removal recipe, symptoms-of-wrong-choice table). Docs-only, syncs to template via `aitana-template-publish`. | fork-ergonomics | ✅ Shipped (2026-06-07, docs-only) |
| G45 / Frictions 25-27 | G43 shipped A2A discovery-compliant cards, but the `url` field returns 405 on actual POST: peers can discover the agent, can't invoke it. Three coupled fork-blocking findings: Friction 27 — ADK's `to_a2a()` registers A2A routes via Starlette lifespan event, which doesn't propagate to FastAPI sub-mounts → every `/a2a/*` returns 404; Friction 25 — Next.js owns the public ingress on Cloud Run multi-container deploys, `/a2a/*` falls through to its catch-all 404 before FastAPI sees it; §2 — G43's well-known proxy strips `card.url` path when rewriting origin, so the new `url = base/a2a` got truncated to bare host. Fix: new `protocols/a2a_invocation.py` with synchronous `build_a2a_app()` helper (works around lifespan loss) + `A2AAuthMiddleware` (JSON-RPC error envelopes on auth failure, not HTML 401). Frontend Next.js `async rewrites()` for `/a2a` + `/a2a/:path*`. Well-known proxy parses upstream URL and swaps only the origin. Cloud Build env vars to enable. New `scripts/simulate-a2a-peer.py` 6-step probe + `verify-a2a.sh` POST invocation check. +6 backend tests + 2 frontend tests. Peers go from "registered but broken" to actually invokable. | a2a-invocation-bridge | ✅ Shipped (2026-06-08) |
| G46 / Frictions 29-30 | G45's `/a2a` invocation surface went live, then within hours hit two coupled file-handling failures in production: (A) peer sends a fresh file but card advertises only `defaultInputModes:["text"]`, so Gemini Enterprise silently strips it at the peer side BEFORE reaching us (real failure 2026-06-07T22:06 UTC — agent saw `document_ids=[]`); (B) peer asks about existing docs in a curated GCS org bucket but no convention exists for "this deploy is bound to this bucket". Plus the silent footgun (Friction 29): even with a `FileExtractionInterceptor` wired via `A2aAgentExecutorConfig(execute_interceptors=[...])`, the interceptor never fires because ADK's executor picks the LEGACY (no-interceptor) path unless `force_new_version=True` is passed OR the peer sends a `X-A2A-Extensions` "new-version" hint — and GE doesn't send the hint. Plus Friction 30: tried to use Discovery Engine Agent resource `metadata` field for per-registration bucket binding, schema rejected it (`INVALID_ARGUMENT: Unknown name "metadata"`). Fix: extend card `defaultInputModes` with PDF/DOCX/XLSX/PPTX/ODT/EML/CSV/TXT MIME types (override via `A2A_AGENT_INPUT_MIME_TYPES` env); new `FileExtractionInterceptor` walks message.parts, validates MIME + size + URI scheme, persists each FilePart as `doc:{id}.json` artifact, STRIPS FileParts from message.parts (prevents double-injection via ADK's native `convert_a2a_part_to_genai_part` to Gemini); existing `make_document_loader` `before_agent_callback` picks up `state["document_ids"]` unchanged — zero skill-code change. New `list_org_documents` + `read_org_document` `FunctionTool`s on `root_agent` when `A2A_AGENT_DOCUMENTS_BUCKET=gs://...` env var is set (SA needs `roles/storage.objectViewer` on bucket). `build_a2a_app()` defaults `force_new_version=True` to lock the new-impl path. All env-gated. 2 new CLI commands (`aiplatform a2a probe-org-bucket`, `aiplatform a2a send-file`). +11 tests (5 file_extraction + 4 org_documents + 2 defaultInputModes). Multi-tenant per-call bucket binding deferred until GE sends peer-identification headers. | a2a-document-support | ✅ Shipped (2026-06-08) |

---

## Template Sync Items (already shipped in Aitana, need to reach the public template)

These are resolved in the Aitana platform but not yet in `sunholo-data/ai-protocol-platform`.
Run the `aitana-template-publish` skill to sync after each PR merges.

| Item | Platform commit / design doc | What to sync |
|------|------------------------------|--------------|
| #16 anonymous-group persistence | v6.2.0 doc `anonymous-group-id-auth.md` | `backend/auth/group_id_auth.py` Firestore persistence |
| #22 A2UI opt-out | AIPLA commit (pending push) | `A2uiToolConfig.enabled` field + agent factory gate |
| #23 flex min-h-0 | platform commit `36ee3cd` + layout.tsx fix | `chat/[...path]/page.tsx` + `app/layout.tsx` |
| #25 defaults tool opt-out | AIPLA commit (pending push) | `toolConfigs.defaults` flags + agent factory gates |

---

## Priority Order

Recommended ship order (each is an independent PR against the template):

1. **tool-opt-out** — already coded, just needs a PR opened (0.5d)
2. **auth-hardening** — prevents silent 403/400 in fresh forks (1d)
3. **cloudbuild-hardening** — unblocks new GCP project setup; now includes G37 artefact auto-deploy (1.5d)
4. **fork-ergonomics** — largest quality-of-life win for downstream forks; **bumped to P0** after gde-ap-agent fork; now includes G23 sanitize/docs sync (5.5d)
5. **protocol-defaults** — function-as-schema flip + callback composition + A2UI per-path patches; highest signal of the 2026-06-03 feedback round (3d)
6. **chat-surface-defaults** — resolves G22 follow-up; Workbench tabs, DefinitionList, audit-pane emit_* fix, avatar theming; depends on protocol-defaults (4d)
7. **session-management** — correctness; `#26` is a one-liner (1d)
8. **dx-hardening** — discoverability + dead code cleanup (1d)
9. **mcp-apps-artefacts** — largest scope; unblocks static-artefact use cases (2d)
10. **mcp-apps-artefact-quality** — sibling of #9; theming + seeded default state; depends on #6 Workbench mount preservation (1.5d)
